# Minimal MVP environment (single-container)
NODE_ENV=development
MVP_MODE=1
WA_LIBRARY=baileys
WA_REMOTE_AUTH_STORE=memory
WA_HTTP_PORT=3005
IDENTIFIER_HASH_SECRET=change_me_identifier_hash_secret

# Optional: set to use pairing code flow
# WA_REMOTE_AUTH_PHONE_NUMBERS=15551234567

# Optional: disable QR fallback
# WA_REMOTE_AUTH_DISABLE_QR_FALLBACK=false
