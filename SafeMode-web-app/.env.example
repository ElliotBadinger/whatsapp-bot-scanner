# SafeMode web app (Next.js)

# Where the web app's server-side API routes can reach the control-plane service.
# Local dev (running control-plane directly): http://localhost:8080
# Docker compose (running inside the same network): http://control-plane:8080
CONTROL_PLANE_BASE=http://localhost:8080

# Used by the web app's server-side proxy routes only.
CONTROL_PLANE_API_TOKEN=

# Admin authentication

# Admin password used for /admin login (>= 12 chars recommended).
SAFEMODE_ADMIN_PASSWORD=

# Secret used to sign admin session cookies (must be >= 32 chars).
SAFEMODE_SESSION_SECRET=

# Optional session TTL in seconds. Default: 43200 (12 hours).
SAFEMODE_SESSION_TTL_SECONDS=43200
