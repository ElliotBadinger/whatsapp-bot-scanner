# SafeMode web app (Next.js)

# Where the web app's server-side API routes can reach the control-plane service.
# Local dev (running control-plane directly): http://localhost:8080
# Docker compose (running inside the same network): http://control-plane:8080
# NOTE: `CONTROL_PLANE_BASE` is deprecated and will be removed in a future release.
# Prefer `CONTROL_PLANE_URL`.
# If `CONTROL_PLANE_URL` is not set, the app falls back to `CONTROL_PLANE_BASE`.
CONTROL_PLANE_URL=http://localhost:8080

# Server-only token used by the web app's `/api/*` routes.
CONTROL_PLANE_API_TOKEN=your-secure-token

# Public values exposed to the browser.
NEXT_PUBLIC_BOT_PHONE_NUMBER=+1234567890
NEXT_PUBLIC_WA_ME_LINK=https://wa.me/1234567890
